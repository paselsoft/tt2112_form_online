# Variabili d'ambiente per TT2112 Digitale
# Copia questo file in .env.local per sviluppo locale

# === EMAIL SMTP CONFIGURATION ===
# Richiesto per l'invio email al Ministero

# Host SMTP (default: smtp.gmail.com)
EMAIL_HOST=smtp.gmail.com

# Credenziali SMTP
# Per Gmail: usa una "App Password" generata da https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here

# Mittente email (opzionale)
EMAIL_FROM="TT2112 Digitale <noreply@tt2112.it>"

# === GOOGLE CLOUD CONFIGURATION ===
# Solo per deploy su Cloud Run

# Project ID del tuo progetto Google Cloud
GCP_PROJECT_ID=your-project-id

# === DEVELOPMENT ===
# Porta locale (opzionale, default: 8080)
PORT=8080

# Environment (opzionale)
NODE_ENV=development
